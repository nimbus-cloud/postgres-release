---
name: one-promote-slave

description: "SSH to slave node, and promote it to has_master_conf status."

templates:
  errand.sh.erb: bin/run

properties:
  slave.node:
    description: "IP address of slave node you wish to promote"
  vcap_password:
    description: "password for vcap user to ssh in to slave node."
